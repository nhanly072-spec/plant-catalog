{
  "name": "plant-catalog",      // Just your project name
  "version": "1.0.0",          // Initial version
  "type": "module",            // Enables ES modules (import/export) in Node.js
  "dependencies": {
    "express": "^4.18.2",      // Web server
    "node-fetch": "^3.3.2"     // fetch() support in Node.js
  },
  "scripts": {
    "start": "node server/server.js"  // Start your server with npm start
  }
}
